struct User {
    id: String,
    first_name: String,
    last_name: String,
    image_link: String,
    email: String, // validate this email properly soon
                   // image_size use imagesize module to get the file size of the image and make sure it isnt
                   // greater than a certain amount
}
